// Forms Module - Simplifié pour Fiche Étoile uniquement
const FormsModule = {
    init: function() {
        console.log('Forms module initialized (Fiche Étoile only)');
        // Les formulaires sont maintenant gérés par FicheEtoileModule
    }
};
